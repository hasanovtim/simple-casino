spring:
  application:
    name: Discovery
server:
  port: 8081
eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
          default-zone: http://localhost:8081
